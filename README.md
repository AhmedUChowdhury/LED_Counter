# LED_Counter
## Overview
In this program we use the Tiva with the Alice EduBase and program it using Arm assembly to show how DMA works. Every 15 seconds a timer interrupt is initiated and it activates the DMA which transfers the data to Port B Data registers to turn on the respective LED.
